# Database
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password
DB_NAME=expense_management

# JWT - Must be quoted if it contains special characters like / or =
JWT_SECRET="HXJGba2ps/ayEh6mD5Juag=="

# Optional: Rate limiting defaults
# RATE_LIMITING_TRUSTED_PROXIES=127.0.0.1,::1
# RATE_LIMITING_PER_USER_FORGOT_PASSWORD=10
# RATE_LIMITING_PER_USER_RESET_PASSWORD=10
# RATE_LIMITING_PER_USER_MFA_VERIFY=10

# Optional: Base URLs
# BASE_URL=http://localhost:8080
# FRONTEND_URL=http://localhost:3000
